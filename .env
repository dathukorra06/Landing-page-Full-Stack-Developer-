# Example .env
# Replace <db_password> with your actual password and keep this file out of source control
MONGO_URI=mongodb://localhost:27017
DB_NAME=rtsp_overlay_demo
DEV_NO_DB=true
HLS_FOLDER=static/hls
# Backend environment variables
# If you're running MongoDB locally, this default will connect to it.
# To use a remote MongoDB (Atlas or other), replace MONGO_URI with your connection string.

# Optional S3 settings (leave empty to use local uploads)
# S3_BUCKET=your-bucket-name
# S3_REGION=us-east-1
